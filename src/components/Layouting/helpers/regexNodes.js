export const regexOnlyPump = new RegExp(/(Pump)/);
export const regexOnlyPipe = new RegExp(/(Pipe)/);
export const regexOnlyCoilCooling = new RegExp(/(Coil:Cooling)/);
export const regexByPass = new RegExp(/(Bypass)/);
export const regexOutlet = new RegExp(/(Outlet)/);
export const regexInlet = new RegExp(/(Inlet)/);